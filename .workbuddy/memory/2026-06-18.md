# 2026-06-18

## V6 mcp.json 配置改为本地 Python 进程模式
- V6 之前是 `streamablehttp` 远程模式 (`type` + `url` + `headers`)
- 仿照 V5 改为本地进程模式 (`command` + `args` + `env`)
- V6: `python -m 3dexperience_mcp.server`，env 用 `3DS_URL` 和 `3DS_TOKEN`
- 同时更新了 `index.html` 代码块和 `mcp.json` 独立文件
